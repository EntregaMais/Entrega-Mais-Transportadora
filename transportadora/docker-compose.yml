version: '3.9'
services:
  app:
    container_name: transportadora_app_container
    build: .
    ports:
      - "7730:7730"
    networks:
      - entregamaisnetwork
    restart: on-failure

  # postgres:
  #   container_name: transportadora_postgres_container
  #   image: postgres
  #   ports:
  #     - "7731:5432"
  #   environment:
  #     POSTGRES_USER: ifpb
  #     POSTGRES_PASSWORD: ifpb
  #     PGDATA: /data/postgres
  #   volumes:
  #     - postgres-data:/data/postgres
  #   networks:
  #     - entregamaisnetwork
  #   restart: unless-stopped

  # pgadmin:
  #   container_name: transportadora_pgadmin_container
  #   image: dpage/pgadmin4
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: ifpb@ifpb.edu.br
  #     PGADMIN_DEFAULT_PASSWORD: ifpb
  #     PGADMIN_CONFIG_SERVER_MODE: 'False'
  #   volumes:
  #     - pgadmin-data:/var/lib/pgadmin
  #    ports:
  #      - "7739:80"
  #   networks:
  #     - entregamaisnetwork
  #   depends_on:
  #     - postgres
  #   restart: unless-stopped

networks:
  entregamaisnetwork:
    name: entregamaisnetwork

volumes:
  transportadora-postgres-data:
  # transportadora-pgadmin-data:
